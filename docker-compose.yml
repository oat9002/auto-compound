version: "3.9"
services:
    auto-compound:
        image: oat9002/auto-compound:latest
        restart: always
        env_file:
            - .env
        volumes:
        - "/etc/timezone:/etc/timezone:ro"
        - "/etc/localtime:/etc/localtime:ro"
